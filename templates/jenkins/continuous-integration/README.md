# Jenkins Continuous Integration

Thu muc nay danh cho `Jenkinsfile` build, test, lint, scan va package ma chua deploy.
Neu pipeline vua build vua tu dong dua artifact len server va chay lai ung dung, hay dat trong `../continuous-deployment/`.
